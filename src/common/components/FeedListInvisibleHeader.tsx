import { View } from 'moti';
import React from 'react';

const invisibleStyle = { width: '100%', height: 1 };

const FeedListInvisibleHeader = () => <View style={invisibleStyle} />;

export default FeedListInvisibleHeader;
