mutation CreateRssFeed($url: String!) {
  createRssFeed(rssFeed: { url: $url }) {
    feedId
    userGuid
    title
    url
    tenantId
    createdAtTimestamp
    lastFetchAtTimestamp
    lastFetchStatus
  }
}
